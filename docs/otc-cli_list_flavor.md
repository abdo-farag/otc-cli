## otc-cli list flavor

List server flavors with pricing (consolidated view)

### Synopsis

List server flavors with pricing across all compute services.

This command provides a consolidated view of:
- ECS (Elastic Cloud Server)
- Compute-optimized (ecsnoc)
- Memory-optimized (memo)
- GPU instances (gpu)
- High-performance (hps)
- Dedicated hosts (deh)

For service-specific pricing with all variants, use:
  otc-cli list pricing <service-name>

```
otc-cli list flavor [flags]
```

### Examples

```
  otc-cli list flavor
  otc-cli list flavor --os windows
  otc-cli list flavor --os redhat
  
  # For detailed ECS-only pricing
  otc-cli list pricing ecs
```

### Options

```
  -h, --help        help for flavor
  -o, --os string   OS type for pricing (openlinux, redhat, oracle, windows) (default "openlinux")
```

### Options inherited from parent commands

```
      --csv              Output in CSV format
      --json             Output raw JSON response (alias)
  -p, --project string   Project ID or name
      --raw              Output raw JSON response
```

### SEE ALSO

* [otc-cli list](docs/otc-cli_list.md)	 - List resources

###### Auto generated by spf13/cobra on 17-Jan-2026
